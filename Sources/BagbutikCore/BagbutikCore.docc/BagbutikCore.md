# ``BagbutikCore``

@Metadata {
    @DisplayName("Bagbutik")
    @PageImage(purpose: icon, source: "Bagbutik-logo.png", alt: "Bagbutik logo")
}

The runtime foundation for Bagbutik's generated App Store Connect API clients.

Use this module to create authenticated requests with ``BagbutikService`` and to work with the shared request, response, and model types.
